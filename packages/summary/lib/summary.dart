/// Resumo do dia entre módulos — `get_daily_summary`, uma das ferramentas
/// mínimas do cérebro (`docs/ARQUITETURA.md:66-67`). Só leitura sobre o
/// Health Data Core compartilhado; nenhuma tabela própria, nenhum estado.
library;

export 'src/daily_summary_tools.dart';
