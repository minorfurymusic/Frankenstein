name: frankstein_summary
description: >-
  Resumo do dia entre módulos (passos, refeições, treino, corrida) — get_daily_summary,
  uma das ferramentas mínimas do cérebro (docs/ARQUITETURA.md:66-67). Só leitura sobre
  o Health Data Core (docs/ARQUITETURA.md), sem tabela própria.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  frankstein_health_core:
    path: ../health_core
  frankstein_tool_registry:
    path: ../tool_registry

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
