name: frankstein_health_core
description: >-
  Health Data Core — SQLite local, fonte da verdade, offline-first (docs/ARQUITETURA.md, .claude/rules/datacore.md).
  Schema HealthEvent append-only (Fase 3).
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  sqlite3: ^2.4.6
  uuid: ^4.5.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
