name: frankstein_brain
description: >-
  Camada do cérebro — roteador determinístico, LLM com tool-calling, validação, confirmação humana (docs/ARQUITETURA.md, .claude/rules/brain.md, ADR-2).
  Pipeline completo, 1 ferramenta provada de ponta a ponta (Fase 5).
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  frankstein_tool_registry:
    path: ../tool_registry

dev_dependencies:
  frankstein_health_core:
    path: ../health_core
  lints: ^5.0.0
  test: ^1.25.0
